
// Bridge file to maintain compatibility with existing components
// This re-exports the simplified theme hook with the same interface
export { useSimplifiedTheme as useTheme } from './use-simplified-theme';
