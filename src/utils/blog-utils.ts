
import { getBlogPostBySlug, getAllBlogPosts } from '@/data/blogData';

// Export these functions to make them available to components that may be using the old import paths
export { getBlogPostBySlug, getAllBlogPosts };
